# ข้อมูลดิบ (PII) — ไม่ commit ขึ้น GitHub
รายชื่อบ้าน1-12.pdf
คิวอาร์โค้ดแต่ละบ้าน/
roster-import.csv
_embed.json

# ไฟล์ที่ผู้จัดตั้งค่า/วางเองในบริการอื่น (เช่น วางใน Google Apps Script) — ไม่ขึ้น GitHub
apps-script/

# build / dev artifacts
node_modules/
_shots/
_*.txt
*.tgz

# QR lib: keep only the file build.mjs needs
package/*
!package/dist/
package/dist/*
!package/dist/qrcode.js
!package/package.json
