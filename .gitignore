# ── ข้อมูลดิบ (PII) — ห้าม commit ขึ้น GitHub ──
รายชื่อบ้าน1-12.pdf
คิวอาร์โค้ดแต่ละบ้าน/
roster-import.csv
attendance-export.csv
supabase/seed-attendance.sql
supabase/seed-students.sql
_embed.json

# ── ความลับ / env ──
.env
.env.local
.env*.local
.dev.vars
.dev.vars.*

# ── ของเก่า (Apps Script ฯลฯ) เก็บไว้ในเครื่อง ไม่ขึ้น GitHub ──
legacy/apps-script/

# ── คู่มือ deploy/setup (เก็บในเครื่อง ไม่ต้องขึ้น GitHub) ──
DEPLOY.md

# ── Next.js / build artifacts ──
node_modules/
.next/
out/
.vercel/
.open-next/
.wrangler/
*.tgz
next-env.d.ts
.DS_Store

# ── ข้อมูลติดต่อสายรหัส (PII) ──
*.xlsx
supabase/seed-sai.sql

# TypeScript incremental build cache
tsconfig.tsbuildinfo
*.tsbuildinfo
